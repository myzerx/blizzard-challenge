import { styled } from '@/styles/stitches.config'

export const BoxContainer = styled('div', {
  display: 'flex',
  width: '48px',
  height: '48px',

  borderRadius: '4px',

  alignItems: 'center',
  justifyContent: 'center',
  gap: '10px',
})
